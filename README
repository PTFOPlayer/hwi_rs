If you want to use it u need only to install PYHW.zip
executable is named PYHW.exe

Multi-platform hardware info app
Curently no support for Radeon and Intel GPU's but with Nvidia GPU's works with full functionality

Needed modules(for linux only):
Tkinter
psutil
